# Usage

Please follow each command before you run the next.

```
git submodule update --init --recursive
make
mkdir -p ~/.config/obs-studio/plugins
cp -R droidcam-plugin ~/.config/obs-studio/plugins
```
